== IN PROGRESS ==

== NEW FUNCTIONALITIES ==
- Handle fetch params
- Clone feed

== FIXES / IMPROVEMENTS ==
- tagArray should be a tree structure
    - assign unique Id to each node
        - prefix to given node Ids, to avoid conflicts
        - prefix to given node CSS classes, to avoid conflicts
    - delete by index should then be by node unique Id
    - will allow children nodes in Pug template
- Single floating edition box for feed contents?

== BUGS ==
